services:
  service-b:
    build:
      context: ./services/service-b
    ports:
      - "3001:3001"
    env_file:
      - ./services/service-b/.env

  service-a:
    build:
      context: ./services/service-a
    ports:
      - "3000:3000"
    env_file:
      - ./services/service-a/.env